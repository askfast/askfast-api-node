var askfast = function() {
  //container for the restclient and dialog builder
}

askfast.RestClient = require('./askfast/askfastrestclient');
askfast.Dialog = require('./dialog/dialog');

module.exports = askfast;